# generated 1970-01-01, Mozilla Guideline v6.0, lighttpd 1.4.82, OpenSSL 3.6.1, intermediate config, PQ: none
# https://ssl-config.mozilla.org/#server=lighttpd&version=1.4.82&config=intermediate&openssl=3.6.1&guideline=6.0&pq=none
#server.port = 80
$SERVER["socket"] == "[::]:80" { }

# select one TLS module: "mod_openssl" "mod_mbedtls" "mod_gnutls" "mod_wolfssl" "mod_nss"
server.modules += ("mod_openssl")

# lighttpd 1.4.56 and later will inherit ssl.* from the global scope if
# $SERVER["socket"] contains ssl.engine = "enable" and no other ssl.* options
# (to avoid having to repeat ssl.* directives in both ":443" and "[::]:443")
$SERVER["socket"] ==     ":443" { ssl.engine = "enable" }
$SERVER["socket"] == "[::]:443" { ssl.engine = "enable" }
ssl.privkey = "/path/to/private_key"
ssl.pemfile = "/path/to/signed_cert_followed_by_intermediates"
ssl.openssl.ssl-conf-cmd = ("MinProtocol" => "TLSv1.2")
#ssl.openssl.ssl-conf-cmd += ("Curves" => "X25519:prime256v1:secp384r1")  # lighttpd 1.4.82 TLS default appends X448

# lighttpd TLS defaults are widely supported by clients and should be preferred.
# See https://wiki.lighttpd.net/Docs_SSL
# Uncomment to better match the less restricted Mozilla intermediate spec.
#ssl.openssl.ssl-conf-cmd += ("CipherString" => "ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305")
